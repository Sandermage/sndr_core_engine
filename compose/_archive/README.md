# Archived compose artifacts

`long-ctx-27b.yml` — compose render of the `long-ctx-qwen3.6-27b` preset,
which was archived to `sndr/model_configs/builtin/presets/_archive/`.
Kept here for reference; restore the preset first if you need to relaunch
this configuration, then re-render with `sndr compose render <preset>`.
